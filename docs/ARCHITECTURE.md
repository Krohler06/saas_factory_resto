Configuration globale :
  config/factory.yml

Configuration métier :
  tenants/private/<client_id>.yml

Templates versionnés :
  template/

Rendu temporaire :
  generated/<client_id>/

Déploiement :
  /opt/clients/<client_id>/

Secrets :
  /opt/clients/<client_id>/.env
